[Unit]
Description = Glances in Web Server Mode
After = network.target

[Service]
ExecStart = /usr{% if inventory_hostname_short == 'debian' %}/local{% endif %}/bin/glances -C /etc/glances/glances.conf -w -u hunteroi

[Install]
WantedBy = multi-user.target
